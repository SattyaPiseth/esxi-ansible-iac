# ESXi 6.7 VM Var Files

Create VM-specific `.pkrvars.hcl` files here only for VMs intentionally built
on the ESXi 6.7 host. Keep placement and resource sizing separate from ESXi 8
to avoid building a VM on the wrong host.
