---
- name: Install Kubespray control node packages
  ansible.builtin.apt:
    name: "{{ kubespray_control_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  become: true
