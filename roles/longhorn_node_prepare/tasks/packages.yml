- name: Install Longhorn node prerequisites
  ansible.builtin.apt:
    name: "{{ longhorn_node_prepare_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  become: true
